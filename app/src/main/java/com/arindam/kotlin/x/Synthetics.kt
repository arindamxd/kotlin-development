package com.arindam.kotlin.x

/**
 * Kotlin is a low ceremony language, the compiler generates synthetic classes, properties,
 * and functions when necessary to keep the language concise 🌤️
 *
 * Created by Arindam Karmakar on 2019-07-24.
 */

// (1/2) Don't have to declare functions inside class.

// (2/2) `main` does not require `args`, if your program doesn't require command-lines arguments.
fun main() {
    println("Hello World!") // > Hello World!
}
